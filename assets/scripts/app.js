/*const deleteButton = document.querySelector(".delete");

deleteButton.addEventListener("click", () => {
  var deletePopUp = confirm("Are you sure you want to delete your profile?");
  if (deletePopUp == true) {
    alert("Your profile is deleted");
  }
  //return deletePopUp;
});*/
